interface:
  display_name: "Wiki Query"
  short_description: "Answer from wiki pages"
  default_prompt: "Use $wiki-query to answer this question from my wiki, citing the pages you read with [[slug]] links."
